from datetime import datetime

class Book:
    def __init__(self, book_id: int, title: str, author: str, date_release, is_borrow: str, ):
        self.book_id = book_id,
        self.title = title,
        self. author = author,
        self.date_release = date_release,
        self.is_borrowe = is_borrow
